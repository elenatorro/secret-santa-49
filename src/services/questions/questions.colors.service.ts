import { questionPlaceholders } from '@/services/questions/questions.service'

const colorQuestions = {
  1: `The ${questionPlaceholders.color} package was bought on ${questionPlaceholders.day}`,
  * The [color] package wasn't bought on [day]
  * The [color] package was bought by the one with [tattoo] tattoo
  * The [color] package contains [present]
  * The [color] package contains [present]
}
